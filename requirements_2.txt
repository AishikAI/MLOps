# Existing dependencies (used in FastAPI)
fastapi==0.115.5
uvicorn==0.18.3  # Ensure Uvicorn is included for running FastAPI
numpy>=1.21.0
pandas>=1.3.0
python-multipart
joblib
scikit-learn>=0.24
mlflow>=2.0
flask
gunicorn
evidently
xgboost
catboost
matplotlib

# Additional dependencies for logging and monitoring (if needed)
google-cloud-logging  # For logging to GCP services
